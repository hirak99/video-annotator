# Example configuration. You can copy this to your config, and use --config to
# point to it.

labels:
  - name: (unknown)
    overlap: true
  - name: blur
    overlap: true
  - name: object_no_1
    overlap: false

videos:
  - video_file: "video.mkv"
    label_file: "labels.json"
  - video_file: "video2.mkv"
    label_file: "labels2.json"

